section
    nav.navbar.navbar-default.categories__nav
        .container
            .navbar-header
                button(type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false")
                    span.sr-only Toggle navigation
                    span.icon-bar
                    span.icon-bar
                    span.icon-bar
                .navbar-brand() V-Wishes!
            #bs-example-navbar-collapse-1.collapse.navbar-collapse
                ul.nav.navbar-nav
                    li
                        a(href="" ng-click="vm.categorySet('all')" data-cat-id="all")
                            | Все
                            span.sr-only (current)
                    li(ng-repeat='category in vm.categories')
                        //- span.sr-only (current)
                        a(href="" ng-click="vm.categorySet(category.id)" data-cat-id="{{category.id}}")
                            | {{category.name}}
                            span.sr-only (current)
                ul.nav.navbar-nav.navbar-right
                    li
                        cart-btn
    cat-content(data-requests="requests" data-categories="vm.categories")